<br></br>
<div class="text-left font-italic ml-5">
<div class="text-dark bg-warning pt-2 pb-2 pl-1 mr-5">
<h2>We offer tutoring in...</h2>
</div>
<br>
<div class="text-secondary">
<% subjects = @instructors.map do |instructor|%>
    <%instructor.subject %>
    <% end %>
<h5>
<strong>
<ul>
<% subjects.uniq.each do |subject| %>
    <li><%= subject%>, taught by:</li>
    <ul>
    <h6><%@instructors.each do |instructor|%>
        <%if instructor.subject == subject%>
           <li> <%=link_to instructor.name, instructor_path(instructor), style: "color: grey;"%></li>
       <% end%>
    <%end%>
    <br>
    </h6>
    </ul>
    <% end %>
    </ul>
    <br>
    </strong>
    </h5>
    </div>